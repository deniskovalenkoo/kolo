# build output
dist/
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production
.env.local

# macOS-specific files
.DS_Store

# editor
.vscode/*
!.vscode/extensions.json
.idea/

# scraper artifacts
scripts/.cache/

# Claude Code runtime artefacts — these are per-machine and contain
# worktree gitlinks / task locks / local settings that shouldn't be
# shared across collaborators.
.claude/worktrees/
.claude/scheduled_tasks.lock
.claude/launch.json
.claude/settings.local.json
